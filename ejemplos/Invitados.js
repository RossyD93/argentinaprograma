let invitadosAdmitidos = [];
let invitadosNoAdmitidos = [];
let invitados =  ['Chris', 'Anne', 'Colin', 'Terri', 'Phil', 'Lola', 'Sam', 'Kay', 'Bruce'];

for (let i = o; i < invitados.length; i++) {
    if (invitados [i] === 'Phil' ||  invitados[i]=== 'Lola') {
    invitadosNoAdmitidos.push(invitados[i]);
   }
else {
    invitadosAdmitidos.push(invitados[i]);
}
}

console.log('invitadosAdmitido: )

